services:
  - type: web
    name: uxlens
    runtime: docker
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false # you'll paste this manually in Render dashboard
    healthCheckPath: /
